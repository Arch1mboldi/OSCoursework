--- a/arch/x86/entry/syscalls/syscall_64.tbl
+++ b/arch/x86/entry/syscalls/syscall_64.tbl
@@ -469,3 +469,6 @@
 # 当前最后一条 common 系统调用是 469 (file_setattr)
+# 紧接着在 469 之后添加，使用 470/471/472:
+470	common	proc_collect		sys_proc_collect
+471	common	proc_snapshot		sys_proc_snapshot
+472	common	proc_stat		sys_proc_stat

# ===== 操作说明 =====
# 1. 当前 syscall_64.tbl 末尾:
#    465  common  listxattrat             sys_listxattrat
#    466  common  removexattrat           sys_removexattrat
#    467  common  open_tree_attr          sys_open_tree_attr
#    468  common  file_getattr            sys_file_getattr
#    469  common  file_setattr            sys_file_setattr
#    512-547 为 x32 历史兼容范围，不可使用
#    548+ 也可用，但 470-472 是自然延续
#
# 2. 在 469 之后、x32 范围之前添加上述三行
# 3. 格式: <号> <abi> <名称> <入口函数>
#    - common = 64位 和 x32 ABI 均可用
#    - 入口函数名与 SYSCALL_DEFINE 宏参数对应:
#      SYSCALL_DEFINE3(proc_collect, ...) -> sys_proc_collect
